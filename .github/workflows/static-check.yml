name: Static check

on:
  pull_request:
  push:
    branches:
      - main
      - master

jobs:
  static-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate static HTML project
        run: npm run check
